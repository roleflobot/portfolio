# 키·토큰 연동 가이드

API 키, 토큰, 비밀번호 같은 자격증명을 누가 다루고 어디에 저장하는지에 대한 공통 규칙입니다. **사람, Claude Code, Codex가 이 문서를 같이 읽고 같은 기준으로 움직이는 것**이 목적입니다. 새 프로젝트를 시작할 때 [AUTOMATION_GUIDE.md](./AUTOMATION_GUIDE.md)와 함께 참고하세요.

---

## 1. 핵심 원칙 한 줄 요약

> **키의 실제 값은 최소한의 사람/장소만 거친다. AI는 "이름"과 "어디에 넣을지"를 다루고, 값 자체는 되도록 CLI가 대신 옮기게 한다.**

---

## 2. 키의 두 종류

| 종류 | 특징 | 예시 | 노출돼도 되는가 |
|---|---|---|---|
| **퍼블릭(공개) 키** | 브라우저에 그대로 실려도 설계상 안전한 키. 서버 쪽 권한 검사(RLS 등)로 보호됨 | `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_*` 접두사 붙은 모든 값 | 노출돼도 큰 문제 없음(단, 그렇다고 코드에 하드코딩할 이유는 없음) |
| **시크릿(비밀) 키** | 서버에서만 써야 하는 키. 유출되면 데이터 전체 접근, 결제, DB 직접 조작 등이 가능해짐 | `SUPABASE_SERVICE_ROLE_KEY`, DB 비밀번호, `GEMINI_API_KEY`, `NAVER_CLIENT_SECRET`, Vercel/GitHub 토큰 | **절대 코드·커밋·채팅·스크린샷에 노출되면 안 됨** |

헷갈리면 "이 값이 그대로 브라우저 개발자도구에 찍혀도 괜찮은가?"로 판단하세요. 괜찮으면 퍼블릭, 아니면 시크릿입니다.

---

## 3. 값이 사는 곳

키는 딱 두 군데에만 존재해야 합니다.

1. **로컬 개발용**: 프로젝트 루트의 `.env.local` (반드시 `.gitignore`에 포함, 커밋 금지)
2. **배포용**: Vercel(또는 해당 배포 플랫폼) 프로젝트의 Environment Variables

이 두 곳 **둘 다** 등록해야 로컬 개발과 배포가 모두 동작합니다. 한쪽만 등록하면 "로컬은 되는데 배포에서만 안 됨"(또는 반대) 문제가 생깁니다.

`.env.local.example`(또는 `.env.example`)에는 **값 없이 변수 이름만** 남겨서 커밋합니다 — 이건 시크릿이 아니라 "이 프로젝트가 어떤 변수를 필요로 하는지 보여주는 문서"입니다.

---

## 4. 값이 흘러가는 3단계

### 1단계 — 발급 (사람이 함)

키는 각 서비스의 대시보드에서 사람이 직접 발급받습니다. AI가 대신 발급받을 수 없습니다(로그인/결제 정보가 필요한 경우가 많음).

| 서비스 | 발급 위치 |
|---|---|
| Supabase | 프로젝트 대시보드 → Project Settings → API |
| Vercel | 대시보드 → 프로젝트 → Settings → Environment Variables (또는 `vercel login`으로 CLI 인증) |
| Gemini | Google AI Studio |
| Naver | 네이버 개발자센터 → 애플리케이션 등록 |
| GitHub | Settings → Developer settings → Personal access tokens (또는 `gh auth login`) |

### 2단계 — 저장 (사람이 하거나, AI가 대신해도 되는 경우)

- **로컬 `.env.local`**: AI가 파일에 직접 써도 됩니다. 이건 로컬 파일 편집일 뿐이고, 이미 이 프로젝트의 표준 방식(`AGENTS.md`/`CLAUDE.md`에 명시)입니다.
- **Vercel 대시보드(웹 화면)**: **AI가 웹 화면의 입력창에 값을 직접 타이핑하는 것은 지양합니다.** 사람이 대시보드에서 직접 입력하는 것이 원칙입니다.
- **Vercel CLI로 값을 안 보고 옮기는 방법(권장)**:
  ```bash
  # Vercel에 이미 등록된 값을 로컬로 그대로 받아오기 — AI가 값을 안 봐도 됨
  npx vercel env pull .env.local

  # 반대로, 로컬 .env.local에 있는데 Vercel에 없는 값을 올릴 때
  npx vercel env add <이름> production
  # → 터미널이 값을 입력하라고 물어보면, 그건 사람이 직접 타이핑
  ```

### 3단계 — 사용 (코드에서)

```ts
// ✅ 항상 이렇게
const key = process.env.GEMINI_API_KEY

// ❌ 절대 이렇게 하지 않기
const key = "AIzaSy..."
```

---

## 5. AI 도구별 차이 — 왜 다르게 동작하는지

같은 작업이라도 도구마다 자격증명을 다루는 방식이 다를 수 있습니다. 어떤 도구를 쓰든, **사람이 그 도구의 방식을 알고 있어야** 합니다.

- **Claude Code**: 결제·로그인 정보, 비밀번호, API 키를 웹 화면의 입력창에 직접 입력하는 것을 원칙적으로 하지 않습니다. 대신 `.env.local` 파일 편집이나 `vercel env pull` 같은, 값이 사람 눈이나 AI 컨텍스트에 그대로 노출되지 않는 방법을 우선 씁니다.
- **Codex(또는 사람이 값을 직접 붙여넣어주는 방식으로 쓰는 도구)**: 사람이 채팅/터미널에 값을 직접 입력해서 넘겨주는 방식을 쓴다면, 그 값이 **터미널 히스토리, 로그, 대화 기록에 그대로 남을 수 있다는 점**을 감안해야 합니다. 이런 방식을 쓸 경우:
  - 넘겨준 뒤에는 셸 히스토리(`history -c` 등)나 대화 기록에서 값이 남지 않는지 한 번 확인
  - 가능하면 시크릿 키보다는, 위 4단계처럼 CLI가 대신 옮기게 하는 방법을 우선 고려
  - 어떤 방식을 택하든 **최종 저장 위치는 3번 섹션과 동일**(`.env.local` + Vercel 대시보드)해야 합니다.

---

## 6. 커밋 전 사고 예방 체크리스트

실제로 겪었던 사례: 초기 설정 스크립트(`scripts/*.js`) 여러 개에 Supabase `service_role` 키와 DB 비밀번호가 그대로 하드코딩되어 있었고, `.gitignore`에는 그중 일부만 표시되어 있었습니다. 아래처럼 직접 스캔해서 찾아냈습니다.

```bash
# 커밋하기 전에, 스테이징된 파일에 진짜 값이 들어있는지 훑어보기
git diff --cached | grep -iE "key|secret|password|token"

# 프로젝트 전체에서 흔한 시크릿 패턴 찾기 (서비스마다 접두사가 다름 — 발급받은 키의 실제 접두사로 바꿔서 검색)
grep -rn "sb_secret_\|service_role\|AIzaSy\|ghp_" --include="*.js" --include="*.ts" .
```

zip으로 묶어서 제출/공유할 때도 같은 방식으로 스캔 후, `.env.local`, `.mcp.json`, `.vercel/`, 하드코딩된 키가 있는 스크립트를 제외하고 압축하세요.

---

## 7. 이미 노출됐다면

- git에 커밋됐다면, 나중에 커밋을 지워도 **그 키는 이미 유출된 것으로 간주**하고 즉시 서비스 대시보드에서 **재발급(rotate)**합니다. git 히스토리에서 지우는 것은 재발급의 대체제가 아닙니다(이미 push된 적이 있다면 GitHub 등 어딘가에 캐시됐을 수 있음).
- 재발급 후에는 `.env.local`과 Vercel 환경변수 양쪽을 새 값으로 갱신하고, 재배포까지 해야 실제로 반영됩니다.
