# Pyeongnaeng Solo Quest
## 평양냉면 혼밥 도장깨기

> 서울의 평양냉면집을 저장하고, 혼자 이용할 수 있는지 확인하며, 방문 여부·개인 평점·메모를 관리하는 개인 웹 서비스

---

## 1. 프로젝트 목적

혼자 평양냉면을 먹으러 가고 싶어도 다음 정보를 한눈에 확인하기 어렵다.

- 혼자 입장해도 되는지
- 1인석이 있는지
- 특정 시간대에는 혼자 이용하기 어려운지
- 실제로 방문한 곳과 아직 가보지 않은 곳이 어디인지

이 프로젝트는 서울의 평양냉면집 후보를 저장하고, 사용자가 직접 혼밥 가능 여부와 방문 경험을 기록하도록 한다.

---

## 2. 핵심 사용자

- 서울에서 혼자 평양냉면을 먹고 싶은 사람
- 가보고 싶은 평양냉면집을 한곳에 정리하고 싶은 사람
- 방문한 식당에 별점과 메모를 남기고 싶은 사람

---

## 3. MVP 목표

로그인한 사용자가 다음 작업을 할 수 있어야 한다.

1. 평양냉면집 목록 조회
2. `가고 싶은 곳`과 `다녀온 곳` 구분
3. 서울 자치구별 필터
4. 식당 상세정보 조회
5. 식당 등록·수정·삭제
6. 혼밥 가능 여부 기록
7. 방문 완료 처리
8. 방문한 식당에 개인 별점과 메모 작성
9. 네이버 지도 링크 열기
10. 자신의 데이터만 조회·수정·삭제

---

## 4. 기술 스택

- **Frontend / Full Stack**: Next.js App Router
- **Language**: TypeScript
- **Database / Auth**: Supabase
- **Styling**: Tailwind CSS
- **Deployment**: Vercel
- **Map**: 지도 API를 삽입하지 않고 외부 네이버 지도 링크만 제공

### 개발 원칙

- Next.js **App Router 기준**으로만 작성한다.
- `pages/`, `getServerSideProps` 등 Pages Router 문법을 사용하지 않는다.
- 서버 컴포넌트를 기본으로 사용한다.
- 상태, 이벤트 핸들러, 브라우저 API가 필요한 작은 컴포넌트에만 `'use client'`를 붙인다.
- API 키와 Supabase 키를 코드에 직접 작성하지 않는다.
- 환경변수는 `.env.local`에서 관리한다.
- 한 번에 모든 기능을 구현하지 말고 단계별로 구현하고 확인한다.

---

## 5. 화면 구성

와이어프레임 이미지 파일:

```text
./pyeongnaeng-wireframe.png
```

Markdown에서 표시하려면:

```md
![평양냉면 혼밥 도장깨기 와이어프레임](./pyeongnaeng-wireframe.png)
```

### 화면 1 — 목록

- 상단 서비스명: `평양냉면 혼밥 도장깨기`
- `가고 싶은 곳` / `다녀온 곳` 탭
  - 가고 싶은 곳: `visited = false`
  - 다녀온 곳: `visited = true`
- 서울 자치구 필터
- 식당명 검색
- 식당 카드 목록
- 카드 표시 항목
  - 식당명
  - 자치구
  - 평양냉면 가격
  - 혼밥 가능 여부
  - 방문 여부
  - 방문한 경우 개인 별점
- `[상세보기]`
- `[+ 식당 등록]`

### 화면 2 — 상세

- 식당명
- 서울 자치구
- 주소
- 평양냉면 가격
- 혼밥 가능 여부
- 방문 여부
- 개인 별점
- 메모
- 사진 (본인만 업로드/변경 가능, 1장)
- `[네이버 지도에서 보기]`
- `[목록]`
- `[수정]`
- `[삭제]`

#### 상세 화면 동작

- `다녀왔어요` 토글을 켜면 `visited = true`
- 방문 완료 상태일 때 별점과 메모를 입력할 수 있다.
- 아직 방문하지 않은 식당은 별점을 비워둘 수 있다.
- 지도 링크는 새 탭에서 연다.

### 화면 3 — 등록/수정

입력 항목:

- 식당명
- 서울 자치구
- 주소
- 평양냉면 가격
- 혼밥 가능 여부
- 방문 여부
- 개인 별점
- 메모
- 네이버 지도 링크
- `[취소]`
- `[저장]`

등록 화면과 수정 화면은 같은 폼 컴포넌트를 재사용한다.

---

## 6. 데이터베이스 설계

### `restaurants`

```text
restaurants
  id               bigint, 자동 증가 (identity)
  name             text, 필수 — 식당 이름
  food             text, 필수 — 항상 '평양냉면' 고정값
  district         text — 서울 자치구
  address          text — 주소
  price            integer — 평양냉면 가격
  solo_status      text, 기본값 '미확인' — 혼밥 가능 여부 확인 방법
  visited          boolean, 기본값 false — 다녀왔는지
  rating           integer (1~5) — 개인 별점, 안 갔으면 null
  memo             text — 혼밥 경험 및 한 줄 메모
  map_url          text — 네이버 지도 링크
  photo_url        text — 업로드한 사진 (Supabase Storage 공개 URL)
  user_id          uuid — 누가 등록했는지 (RLS로 본인 데이터만 접근 제한)
  created_at       timestamp, 기본값 now()
```

`district`, `user_id` 등은 실제로는 필수값이지만 DB 제약이 아니라 API 라우트의 입력값 검증과 RLS 정책으로 강제한다(등록 폼에서 비워둘 수 없게 막음).

사진은 Supabase Storage의 `restaurant-photos` 버킷(공개 읽기)에 `{user_id}/{restaurant_id}-*.jpg` 형태 경로로 저장한다. 본인 소유 경로에만 업로드·수정·삭제할 수 있도록 스토리지 정책을 둔다.

### 실제 적용된 SQL

```sql
create table restaurants (
  id bigint generated by default as identity primary key,
  name text not null,
  food text not null default '평양냉면',
  district text,
  address text,
  price integer,
  solo_status text default '미확인',
  visited boolean default false,
  rating integer check (rating between 1 and 5),
  memo text,
  map_url text,
  photo_url text,
  user_id uuid,
  created_at timestamp default now()
);
```

### `solo_status` 허용값

혼밥(1인석) 가능 여부를 어떻게 확인했는지 나타낸다.

- `1인석 있음 (네이버 지도 정보)` — 네이버 지도의 좌석·공간 정보에서 1인석을 확인함
- `1인석 있음 (직접 확인)` — 실제 방문해서 직접 확인함
- `미확인` — 아직 확인하지 않음

---

## 7. 유효성 검사

- `name`: 필수
- `district`: 필수
- `price`: 0보다 큰 정수 또는 비워두기
- `rating`: 1~5 또는 `null`
- `visited = false`이면 `rating`은 `null`로 저장
- `map_url`: 입력된 경우 URL 형식 확인
- `solo_status`: 지정된 세 가지 값 중 하나만 허용

---

## 8. 인증과 권한

### 인증

Supabase Auth의 이메일·비밀번호 회원가입 및 로그인을 사용한다.

구현 범위:

- 회원가입
- 로그인
- 로그아웃
- 로그인하지 않은 사용자는 로그인 페이지로 이동

제외:

- 소셜 로그인
- 프로필 사진
- 닉네임 편집
- 비밀번호 재설정 화면 커스터마이징

### RLS

사용자는 자신의 식당 데이터만 조회·등록·수정·삭제할 수 있어야 한다.

정책 개념:

```sql
auth.uid() = user_id
```

필요 정책:

- SELECT
- INSERT
- UPDATE
- DELETE

클라이언트가 임의의 `user_id`를 넣지 않도록 현재 로그인 사용자의 ID를 사용한다.

---

## 9. 페이지 및 라우트

```text
/login
  회원가입 / 로그인

/
  식당 목록

/restaurants/new
  새 식당 등록

/restaurants/[id]
  식당 상세

/restaurants/[id]/edit
  식당 수정
```

---

## 10. 권장 컴포넌트

```text
components/
  Header.tsx
  RestaurantCard.tsx
  RestaurantList.tsx
  RestaurantFilters.tsx
  RestaurantForm.tsx
  VisitedToggle.tsx
  RatingInput.tsx
  EmptyState.tsx
```

가능한 한 컴포넌트를 작게 나누되, MVP 단계에서 과도하게 추상화하지 않는다.

---

## 11. 초기 데이터 후보

다음 식당들은 사용자가 직접 조사한 초기 후보이다. 주소·자치구·지도 링크가 빠진 항목은 등록 전에 추가 확인한다.

| 식당명 | 평양냉면 가격 | 비고 |
|---|---:|---|
| 능라도 명동 | 17,000원 | 100% 순메밀 |
| 경기옥 | 15,000원 | 신규 오픈 |
| 대엽 을지로 | 13,000원 | 녹두지짐이·육전 |
| 서관면옥 신세계백화점 본점 | 17,000원 | 제주메밀 100% |
| 북창옥 시청 본점 | 15,000원 | 곰탕·불고기 |
| 서령 본점 | 17,000원 | 빕구르망 |
| 평안면옥 | 13,000원 | 혼냉세트 |
| 서관면옥 교대본점 | 17,000원 | 제주메밀 100% |
| 혜준당 논현점 | 14,900원 | 진한 고기육수 |
| 우주옥 | 16,000원 | 어복쟁반 |

초기에는 5~10개만 입력해도 된다. 데이터 수집보다 CRUD·인증·권한·배포 완성이 우선이다.

---

## 12. 구현 순서

### 1단계 — 프로젝트 기본 설정

- Next.js App Router + TypeScript + Tailwind
- Supabase 클라이언트 설정
- `.env.local.example` 작성
- 기본 레이아웃 작성

### 2단계 — 데이터 읽기

- `restaurants` 테이블 생성
- 테스트 데이터 3개 입력
- 목록 페이지에서 데이터 조회
- 식당 카드 표시
- 로딩·빈 목록·오류 상태 처리

### 3단계 — 데이터 쓰기

- 새 식당 등록 폼
- 입력값 검증
- 저장 후 상세 또는 목록 페이지로 이동

### 4단계 — 상세·수정·삭제

- 상세 페이지
- 수정 폼
- 삭제 확인
- 삭제 후 목록으로 이동

### 5단계 — 필터와 방문 기록

- 가고 싶은 곳 / 다녀온 곳 탭
- 자치구 필터
- 검색
- 방문 완료 토글
- 방문한 경우 별점과 메모 입력

### 6단계 — 로그인과 RLS

- 이메일 회원가입
- 로그인·로그아웃
- `user_id` 연결
- RLS 정책 적용
- 다른 사용자의 데이터가 노출되지 않는지 확인

### 7단계 — 마무리와 배포

- 모바일·데스크톱 반응형 확인
- 입력 오류 메시지
- 404 및 데이터 없음 처리
- Vercel 배포
- 배포 환경변수 등록
- README 작성

---

## 13. 안 만들 것

이번 MVP에서는 다음 기능을 구현하지 않는다.

- 앱 내부 지도 API
- 지도 위 다중 마커
- 네이버 지도 자동 검색
- 웹 크롤링 및 실시간 정보 수집
- 댓글 및 공개 리뷰
- 팔로우·친구·공유 기능
- AI 맛집 추천
- 예약 기능
- 실시간 영업시간
- 회사·부서별 투표
- 다른 사용자의 실시간 랭킹

대신 상세 화면에서 **외부 네이버 지도 링크**를 제공한다.

---

## 14. 완료 조건

- [ ] 회원가입 및 로그인
- [ ] 로그인 사용자별 데이터 분리
- [ ] 식당 목록 조회
- [ ] 가고 싶은 곳 / 다녀온 곳 탭
- [ ] 자치구 필터
- [ ] 식당 상세 조회
- [ ] 식당 등록
- [ ] 식당 수정
- [ ] 식당 삭제
- [ ] 방문 완료 토글
- [ ] 별점 1~5 입력
- [ ] 메모 입력
- [ ] 네이버 지도 링크 열기
- [ ] RLS 적용
- [ ] Vercel 배포 완료

---

## 15. Claude Code 작업 규칙

Claude Code는 다음 규칙을 반드시 지킨다.

1. 한 번에 전체 앱을 완성하려 하지 않는다.
2. 위 구현 순서대로 한 단계씩 진행한다.
3. 각 단계가 끝나면 변경한 파일 목록과 역할을 설명한다.
4. 실행 명령과 확인 방법을 제공한다.
5. 에러를 숨기지 말고 원인을 설명한다.
6. 기존에 정상 동작하는 기능을 불필요하게 다시 작성하지 않는다.
7. 위험한 삭제·초기화 명령을 실행하기 전에 반드시 사용자에게 알린다.
8. Supabase 비밀 키를 브라우저 코드에 넣지 않는다.
9. `service_role` 키를 사용하지 않는다.
10. 코드 생성 후 핵심 코드의 역할을 3줄 내외로 설명한다.
11. 구현이 끝난 단계마다 Git 커밋을 권장한다.
12. 디자인보다 기능 완성을 우선한다.

---

## 16. 첫 작업 요청

Claude Code는 먼저 아래 작업만 수행한다.

> 이 기획서를 읽고 프로젝트 구조를 검토한 뒤, 아직 코드를 대량 생성하지 말고 다음을 제안하라.
>
> 1. 구현 단계별 체크리스트  
> 2. 필요한 페이지와 컴포넌트  
> 3. Supabase 테이블 및 RLS 설계  
> 4. 예상되는 주요 오류와 예방 방법  
>
> 제안이 승인된 뒤 1단계부터 구현한다.
