# 미니프로젝트3 — Daily English Hub

## 배포 주소

https://daily-english-hub-liard.vercel.app/

(시크릿 창에서도 로그인 없이 바로 사용 가능합니다.)

## 서비스 설명

**서비스명**: Daily English Hub

**한 줄 소개**: 매일 아침 자동으로 선정된 오늘의 뉴스 5건을 골라 읽으면, AI가 그 뉴스로 영어 요약·어휘·문제·영작 첨삭까지 만들어주는 시사 영어 학습 서비스.

**타깃 사용자**: 매일 시사 뉴스로 영어 실력을 늘리고 싶은 학습자. 로그인 없이도 바로 시작할 수 있게 만들어 진입 장벽을 최소화했다.

**핵심 흐름**:
1. 홈 화면에 오늘(KST) 뉴스 5건이 카테고리(정치/국제/사회/경제/과학기술/문화)와 함께 뜬다. 이 5건은 새벽 3시 배치가 Google 뉴스 헤드라인 중에서 Gemini로 선정해 하루 동안 모든 사용자에게 동일하게 캐싱한다.
2. 뉴스를 고르면 Gemini가 만든 학습자료 화면으로 이동한다. 로그인 안 한 사용자는 첫 클릭 시 닉네임만 정하면(또는 구글/이메일 로그인) 바로 시작할 수 있다.
3. 학습자료는 5단계로 구성된다.
   - Section 1 단어: 지문 핵심 어휘 10개(품사·불규칙 동사 활용형 포함)
   - Section 2 읽기: 영어 요약 5문장 + 해석 토글 + TTS 낭독 버튼(새벽 배치가 미리 만들어둔 음성이라 즉시 재생)
   - Section 3 참/거짓 퀴즈, Section 4 빈칸 채우기 — 각각 독립적으로 제출·채점되고, 지문을 그대로 베낀 게 아니라 paraphrase된 문장으로 출제해 단순 암기로는 못 풀게 했다
   - Section 5 영작: Gemini가 뽑은 필수 단어 5개를 전부 사용해 뉴스 요약 문장을 직접 영작하면, Gemini가 실시간으로 첨삭·피드백을 준다
4. 학습 기록은 Supabase에 저장되어 새로고침해도 남고, "복습" 메뉴에서 지난 주 단위로 그동안 배운 단어·문장을 무작위(중복 없이) 다시 풀어볼 수 있다.

## 기획서 대비 변경 사항 (REQ-01)

원래는 매일 날씨 정보도 영어 표현·대화문으로 바꿔 학습자료에 포함하려 했으나, 날씨 API 연동과 그 데이터를 학습자료 형태로 가공하는 처리 과정이 이번 미니프로젝트 기간에 넣기엔 복잡도가 높아 이번 범위에서는 제외했다. 대신 뉴스 기반 학습 흐름(요약·퀴즈·빈칸·영작·복습·TTS)의 완성도를 높이는 데 집중했다.

## 요구사항 충족 내역

| 요구사항 | 충족 방법 |
|---|---|
| REQ-01 주제 | 시사 뉴스 기반 영어 학습 서비스. 범위 축소 내역은 위 참고 |
| REQ-02 AI 기능 | (1) Gemini로 뉴스 원문 기반 요약/어휘/퀴즈/빈칸/영작 소재 생성 (2) Gemini TTS로 지문 낭독 음성 생성 (3) 사용자가 직접 쓴 영작문을 Gemini가 실시간으로 첨삭·피드백 — 셋 다 실제 입력을 받아 AI가 처리하고 화면에 결과가 나타남 |
| REQ-03 데이터 저장 | Supabase `learning_sessions`(학습 세션·정답·점수), `daily_topics`(오늘의 뉴스 캐시), `topic_audio`(TTS 캐시), `review_progress`(복습 중복 방지) 등 다중 테이블 설계. 로그인 없이 익명 인증으로도 본인 기록만 저장·조회됨(RLS) |
| REQ-04 배포 | Vercel 배포, 함수 리전을 Supabase DB와 같은 시드니로 맞춰 응답 속도 확보 |
| REQ-05 사용성 | 홈 화면에 안내 문구 명시, AI 호출 실패/빈 입력 시 에러 메시지로 처리(크래시 없음), 새벽 배치 진행 중에는 "업데이트 중" 안내로 사용자마다 다른 자료를 보는 상황 방지 |

## 개발 중 겪은 문제와 해결

### 1) 로그인 방식 — 카카오/네이버 제거, 익명 인증으로 전환
초기엔 카카오/네이버 로그인도 지원하려 했으나, 공유 Supabase 프로젝트의 리다이렉트 설정 문제에 더해 카카오는 앱이 비즈니스 앱이 아니어서 이메일 동의 항목 자체를 켤 수 없는 근본적 제약이 있었다. 시간 대비 해결 가능성이 낮아 카카오·네이버는 제외하고 구글·이메일 로그인만 남겼다.
게스트도 서로 구분되어야 한다는 요구를 만족시키기 위해, Supabase의 **익명 로그인**을 도입해 첫 방문 시 자동으로 고유 `user_id`를 부여하고 그 위에 닉네임을 얹었다. RLS를 `auth.uid() = user_id`로 강화해 게스트 간에도 서로의 기록이 보이지 않게 했다(초기엔 `user_id is null`을 열어둬서 모든 게스트가 서로의 기록을 볼 수 있었던 버그를 발견해 수정).

### 2) 새벽 배치와 홈 화면 로딩이 느려진 문제
오디오(TTS)를 캐시에 추가하면서 홈 화면이 매번 불러오는 `daily_topics` 캐시 안에 오디오(약 200~300KB × 5)까지 같이 들어가, 아무도 안 쓰는 홈 화면 요청마다 1MB 넘게 전송되어 느려졌다. 오디오를 별도 테이블(`topic_audio`)로 분리하고, 여기에 Vercel 함수 리전이 Supabase DB(호주 시드니)와 멀리 떨어진 미국 동부로 되어 있던 것도 시드니로 맞춰, 홈 화면 응답이 4~6초에서 0.3~0.5초로 개선됐다.

### 3) 지문을 재생성했는데 오디오가 옛날 그대로인 문제
학습자료 스키마를 여러 번 확장하면서(품사 태그, 보기 해설 등) 지문(summary) 텍스트가 매번 새로 생성됐는데, 오디오는 "이미 있으면 스킵"하는 로직이라 옛 지문을 읽는 음성이 남아있는 버그가 있었다. 지문이 새로 만들어질 때는 오디오도 반드시 다시 만들도록 고쳤다.

### 4) 빈칸 채우기 보기들이 전부 비슷한 뜻이라 정답이 모호한 문제
Gemini에게 "오답도 그럴듯하게"라고만 지시했더니, 4개 보기가 전부 동의어에 가까워 문장에 어느 걸 넣어도 말이 되는 문제가 생겼다(예: succeeded/prevailed/dominated/triumphed). 프롬프트에 "나머지 3개는 문맥상 명백히 틀려야 하며 서로 동의어면 안 된다"는 규칙과 좋은 예/나쁜 예를 추가해 해결했다. 또한 정답이 항상 보기 1번으로 오는 모델의 습관이 그대로 노출되던 것도, 화면에 표시하기 직전 클라이언트에서 무작위로 섞도록 고쳤다.

### 5) 복습 문제의 오답 보기가 품사까지 섞여 너무 쉬웠던 문제
복습 퀴즈는 그 주에 배운 단어 풀에서 무작위로 오답을 뽑았는데, 품사가 달라(예: 정답이 동사인데 오답이 부사) 학습자가 뜻을 몰라도 품사만 보고 정답을 유추할 수 있었다. 어휘 생성 시 품사(pos) 태그를 함께 받도록 스키마를 확장하고, 복습 문제 오답은 같은 품사에서 우선 뽑도록 수정했다.

## 사용 기술

Next.js 16(App Router), TypeScript, Tailwind CSS v4, Supabase(Postgres, Auth-익명 로그인 포함, RLS), Google Gemini API(텍스트 생성 + TTS), Naver 뉴스 검색 API, Google 뉴스 RSS, Vercel(배포 + Cron)

이번 프로젝트는 Claude Code와 함께 페어 프로그래밍 방식으로 진행했다. 서비스 방향·우선순위·범위 축소 판단(날씨 기능 제외 등), 발견된 버그의 재현·검증, 디자인 방향 승인은 직접 진행했고, 코드 작성과 배포, 실전 테스트(브라우저 자동화로 실제 클릭·제출까지 확인)는 Claude Code가 맡아 진행했다.
