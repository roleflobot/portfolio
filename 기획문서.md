# 기획 문서 — 사주 & 고민 상담

## 서비스 개요

- **서비스명**: 사주 & 고민 상담
- **한 줄 설명**: 생년월일(사주)을 입력하면, 사주풀이·오늘의 운세·고민(진로) 상담·명언·행운의 음식까지 Gemini가 답해주는 웹 서비스
- **사용한 AI**: Google Gemini API (`gemini-3.5-flash-lite`, REST/SDK 호출은 서버(FastAPI)에서만 수행)

## 무엇을 입력받고, AI에게 무엇을 묻고, 결과를 어떻게 보여줄까

| 기능 | 입력 | Gemini에게 요청하는 것 | 출력 |
|---|---|---|---|
| 사주 보기 | 생년월일, 태어난 시간(선택), 성별 | 성격/올해의 운세/조언을 소제목별로 간결하게 | 카드형 결과 박스 |
| 오늘의 운세 | 생년월일, 성별 (+ 서버가 오늘 날짜 추가) | 오늘 하루의 총운과 주의할 점 | 결과 박스 |
| 고민 상담 | 생년월일, 성별, 고민 텍스트 | 사주 성향 → 공감 → 사주 기반 조언(진로면 구체적 방향 포함) | 결과 박스 |
| 오늘의 한마디 | 생년월일, 성별 (+ 오늘 날짜) | 사주/오늘 운세에 어울리는 **실존하는** 명언·일화 + 출처 (지어내기 금지) | 결과 박스 |
| 오늘의 행운 음식 | 생년월일, 성별, 지역(도시 선택) | 지역의 실시간 날씨(Open-Meteo API)와 계절, 사주를 종합해 음식 추천 | 결과 박스 |

## 기술 구조

```
브라우저 (saju.html + saju.js)
        │  fetch POST /api/saju, /api/today, /api/worry, /api/quote, /api/food
        ▼
FastAPI 서버 (main.py)
        │  GEMINI_API_KEY는 .env에서 읽어 서버에만 보관
        ▼
Google Gemini API  /  Open-Meteo 날씨 API
```

- **서버-클라이언트 구조**: `main.py`(FastAPI)가 정적 파일(`saju.html`, `saju.js`)을 서빙하고, 브라우저는 자신의 서버(`/api/*`)만 호출합니다. API 키는 브라우저에 절대 전달되지 않습니다.
- **실행 방법**: `python main.py` (또는 `uvicorn main:app --reload`, Windows에서는 `run.bat`으로도 실행 가능)
- **안정성**: 빈 값 입력 시 서버가 죽지 않고 400/422로 응답, 요청 중에는 버튼이 비활성화되어 중복 요청을 방지합니다.

## 막혔던 지점과 해결

1. **답변이 문장 중간에 잘림**: 고민 상담처럼 답변이 길어지는 기능에서 `max_output_tokens` 제한(300)에 걸려 텍스트가 중간에 끊겼습니다. → 해당 기능만 토큰 한도를 500으로 늘려 해결했습니다.
2. **`run.bat` 더블클릭 시 서버 연결 거부(`ERR_CONNECTION_REFUSED`)**: 브라우저를 고정된 2초 뒤에 여는 방식이었는데, 서버가 그보다 늦게 뜨는 경우가 있었습니다. 또한 경로에 한글 폴더명이 포함되어 중첩된 콘솔 창 사이에서 인코딩이 꼬여 `cd` 명령이 실패하기도 했습니다. → 고정 대기 대신 `curl`로 서버가 실제로 응답할 때까지 반복 확인 후 브라우저를 열도록 변경하고, 경로 지정도 `start`의 `/D` 옵션으로 바꿔 인코딩 문제를 피했습니다.
3. **모델 이름 오류**: 처음 사용한 `gemini-2.5-flash` 모델이 더 이상 신규 사용자에게 지원되지 않는다는 오류(404)가 발생했습니다. → 현재 사용 가능한 `gemini-3.6-flash`/`gemini-3.5-flash-lite`로 교체했습니다.
4. **API 키가 잘못된 파일에 저장됨**: 실제 키를 `.env`가 아닌 `.env.example`(gitignore 대상이 아닌 파일)에 붙여넣는 실수가 있었습니다. → 커밋 이력을 확인해 노출되지 않았음을 검증하고, 즉시 올바른 `.env`로 옮겼습니다.
